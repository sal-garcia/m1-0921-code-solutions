/* exported isUpperCased */
function isUpperCased(word) {
  var bool = false;
  if (word === word.toUpperCase()) {
    bool = true;
  }
  return bool;
}

// define a function
// define a variable with a false boolean
// define an if condition checking if all of the characters in the word parameter are uppercased
// if they are change the boolean value to true
// return the bool variable
