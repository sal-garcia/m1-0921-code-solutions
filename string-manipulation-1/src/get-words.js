/* exported getWords */
function getWords(string) {
  var storage = string.split(' ');

  if (string === '') {
    storage = [];
  }
  return storage;
}
// define a function
// define a storage variable
//
