/* exported isLowerCased */
function isLowerCased(word) {
  var bool = false;
  if (word === word.toLowerCase()) {
    bool = true;
  }
  return bool;
}
// define a function
// define a variable with the value of false
// check if each character is lowercased in the word parameter
// if it is change the variable to true
// return the bool variable
