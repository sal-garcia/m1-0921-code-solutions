/* exported toObject */
function toObject(keyValuePair) {
  var obj = {};
  obj[keyValuePair[0]] = keyValuePair[1];
  return obj;
}
// define the function
// define a storage object variable
// assign the element of keyvaluepair at index[1]
// to the object at the element in index 0 of keyvaluepair
// return that obj
