/* exported titleCase */

/*

function titleCase(title) {
  var word = title.split(' ');
  for (var i = 0; i < word.length; i++) {
    if (word[i] === 'if') {
      word[i] = word[i].toLowerCase();
    } if (word[i] === 'in') {
      word[i] = word[i].toLowerCase();
    } if (word[i] === 'of') {
      word[i] = word[i].toLowerCase();
    } if (word[i] === 'the') {
      word[i] = word[i].toLowerCase();
    } if (word[i] === 'for') {
      word[i] = word[i].toLowerCase();
    }
    console.log(word[i])
    else {
      word[i][0].toUpperCase() + word[i].slice(1, word[i].length).toLowerCase();
    }
  }
}

*/
